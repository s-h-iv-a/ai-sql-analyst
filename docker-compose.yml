services:
  backend:
    build:
      context: ./backend

    container_name: ai-sql-analyst-backend

    ports:
      - "8005:8005"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app

    restart: always