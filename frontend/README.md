# React frontend

This folder will contain the React UI for the AI SQL Analyst.

Flow:
1. User enters a natural language question
2. Frontend calls `POST /query`
3. Displays SQL, results table, AI explanation, and Plotly chart
